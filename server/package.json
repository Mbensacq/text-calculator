{
  "name": "text-calculator-sync",
  "version": "1.0.0",
  "description": "Serveur de synchronisation temps réel (SSE + REST) pour Text Calculator. Stockage fichier sans dépendance, ou MySQL/MariaDB/PostgreSQL.",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "engines": { "node": ">=16" },
  "scripts": {
    "start": "node server.js"
  },
  "optionalDependencies": {
    "mysql2": "^3.11.0",
    "pg": "^8.12.0"
  }
}
