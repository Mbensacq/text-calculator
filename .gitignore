# Dependencies (this project is dependency-free, but just in case)
node_modules/

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs & temp
*.log
.cache/

# Sync server runtime data
server/data/

# Secrets — never commit real environment files (only *.example is tracked)
.env
.env.*
!.env.example
server/.env
server/.env.*
!server/.env.example
